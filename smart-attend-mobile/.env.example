# Point the mobile app at your API.
# Leave unset in local Expo (__DEV__) to use localhost / Android emulator defaults.
# Production / release builds always use https://www.smartattend.co unless this is set.

EXPO_PUBLIC_API_URL=https://www.smartattend.co
